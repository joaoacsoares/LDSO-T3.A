@model IEnumerable<places4all.Models.Incapacidade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Designation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Designation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IncapacidadeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.IncapacidadeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IncapacidadeId })
        </td>
    </tr>
}

</table>
