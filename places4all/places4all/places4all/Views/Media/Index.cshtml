@model IEnumerable<places4all.Models.Media>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MediaLink)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MediaDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MediaLink)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MediaDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MediaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MediaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MediaId })
        </td>
    </tr>
}

</table>
